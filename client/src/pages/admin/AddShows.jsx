import React,{useState} from 'react'; import AdminLayout from '../../components/AdminLayout'; import {dummyShowsData} from '../../assets/assets.js';
export default function AddShows(){const [selected,setSelected]=useState(dummyShowsData[0].id);return <AdminLayout><h1>Add <span className="pink">Shows</span></h1><h3>Now Playing Movies</h3><div className="admin-movies">{dummyShowsData.map(m=><button className={selected===m.id?'chosen':''} onClick={()=>setSelected(m.id)} key={m.id}><img src={m.poster_path}/><span>{m.title}</span></button>)}</div><div className="show-form"><label>Show Price<input type="number" defaultValue="49"/></label><label>Select Date & Time<input type="datetime-local"/></label><button className="primary">Add Show</button></div></AdminLayout>}
